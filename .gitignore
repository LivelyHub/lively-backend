# Secrets — never commit
.env
.env.*
!.env.example
*.key
*.pem

# Node / JS
node_modules/
dist/
build/
*.tsbuildinfo

# Uploaded photos (local disk storage, see src/shared/uploads.ts).
# Anchored to the repo root: a bare "uploads/" also ignored
# src/modules/uploads/, which silently kept that module out of the
# refactor commit and broke CI.
/uploads/

# OS / editor
.DS_Store
*.log
.idea/
.vscode/

# Local planning artifacts (specs/plans) — kept private during the competition
docs/superpowers/
.superpowers/

# Local session handoff (not published)
HANDOFF.md
